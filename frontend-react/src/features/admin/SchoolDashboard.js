import React from 'react';
import { Typography } from '@mui/material';

export default function SchoolDashboard() {
  return (
    <div>
      <Typography variant="h5">School Dashboard</Typography>
      <Typography>Admin dashboard content.</Typography>
    </div>
  );
}
