import React from 'react';
import { Typography } from '@mui/material';

export default function SignupPage() {
  return (
    <div>
      <Typography variant="h5">Sign Up</Typography>
      <Typography>Signup form goes here.</Typography>
    </div>
  );
}
