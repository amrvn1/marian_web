import React from 'react';
import { Typography } from '@mui/material';

export default function PaymentPage() {
  return (
    <div>
      <Typography variant="h5">Payment</Typography>
      <Typography>Payment flow and provider selection here.</Typography>
    </div>
  );
}
