import React from 'react';
import { Typography } from '@mui/material';

export default function SearchResults() {
  return (
    <div>
      <Typography variant="h5">School Search Results</Typography>
      <Typography>Results will show here.</Typography>
    </div>
  );
}
